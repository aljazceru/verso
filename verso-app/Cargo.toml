[package]
name = "verso-app"
version = "0.1.0"
edition = "2021"

[dependencies]
verso-core = { path = "../verso-core" }
dioxus = { version = "0.7", features = ["desktop"] }
tokio = { version = "1", features = ["full"] }
